# Phase 1 — copy to .env.local (optional for UI-only starter)

NODE_ENV=development

# Display name in client (optional)
NEXT_PUBLIC_APP_NAME=Legendary Hunts

# Logging: debug | info | warn | error
LOG_LEVEL=info

# When you attach Postgres / Supabase (later phases)
# DATABASE_URL=postgresql://user:pass@localhost:5432/legendary_hunts
